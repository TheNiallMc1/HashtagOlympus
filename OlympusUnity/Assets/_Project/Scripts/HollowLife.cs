using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HollowLife : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // mod test
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
